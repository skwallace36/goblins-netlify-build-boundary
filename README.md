# Netlify build-boundary fixture

Owned public test repository for the Netlify HackerOne program. It emits only
marker metadata and a boolean indicating whether a harmless canary matched a
known digest. It never publishes the canary value.
